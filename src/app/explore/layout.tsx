export const metadata = { title: "Explore", description: "Discover fashion writers and articles across style, runway, streetwear and more." };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}